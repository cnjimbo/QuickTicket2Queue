#!/bin/sh
# post-checkout args: <previous HEAD> <new HEAD> <isBranchCheckout>
# Only run on branch checkout/switch (1), skip file checkout (0).
if [ "$3" = "1" ]; then
  pnpm exec tsx scripts/auto-bump-version-by-branch.ts
fi
